---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: odooinstances.bemade.org
spec:
  group: bemade.org
  names:
    kind: OdooInstance
    listKind: OdooInstanceList
    plural: odooinstances
    shortNames:
    - odoo
    singular: odooinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.url
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: OdooInstance is the Schema for the odooinstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OdooInstanceSpec defines the desired state of OdooInstance.
            properties:
              adminPassword:
                description: adminPassword is the Odoo database administrator password.
                type: string
              configOptions:
                additionalProperties:
                  type: string
                description: configOptions are additional key-value pairs appended
                  to odoo.conf.
                type: object
              database:
                description: database identifies the PostgreSQL cluster to use for
                  this instance.
                properties:
                  cluster:
                    description: |-
                      cluster is the name of the PostgreSQL cluster from the postgres-clusters secret.
                      Defaults to the cluster marked as default.
                    type: string
                type: object
              filestore:
                description: filestore configures persistent storage for the Odoo
                  filestore.
                properties:
                  storageClass:
                    default: standard
                    description: storageClass is the StorageClass to use for the filestore
                      PVC.
                    type: string
                  storageSize:
                    default: 2Gi
                    description: storageSize is the PVC size requested for the filestore
                      (e.g. "10Gi").
                    type: string
                type: object
              image:
                default: odoo:18.0
                description: image is the Odoo container image to use (e.g. "odoo:18.0").
                type: string
              imagePullSecret:
                description: imagePullSecret is the name of the Secret containing
                  registry credentials.
                type: string
              ingress:
                description: ingress configures the Ingress resource for this instance.
                properties:
                  class:
                    description: class is the IngressClass to use (e.g. "nginx", "traefik").
                    type: string
                  hosts:
                    description: hosts are the fully qualified domain names at which
                      this instance is reachable.
                    items:
                      type: string
                    minItems: 1
                    type: array
                  issuer:
                    description: issuer is the name of the cert-manager ClusterIssuer
                      used to issue the TLS certificate.
                    type: string
                required:
                - hosts
                - issuer
                type: object
              probes:
                description: probes configures the HTTP health check paths for Kubernetes
                  liveness, readiness, and startup probes.
                properties:
                  livenessPath:
                    default: /web/health
                    description: livenessPath is the HTTP path used for the liveness
                      probe.
                    type: string
                  readinessPath:
                    default: /web/health
                    description: |-
                      readinessPath is the HTTP path used for the readiness probe.
                      Use /health/ready if the health_check_k8s module is installed for deep checks (DB + filestore).
                    type: string
                  startupPath:
                    default: /web/health
                    description: startupPath is the HTTP path used for the startup
                      probe.
                    type: string
                type: object
              replicas:
                default: 1
                description: replicas is the number of Odoo pod replicas to run.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: resources sets CPU and memory requests and limits for
                  the Odoo container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              strategy:
                description: strategy defines the Deployment update strategy.
                properties:
                  rollingUpdate:
                    description: rollingUpdate configures rolling update parameters.
                      Only applicable when Type is RollingUpdate.
                    properties:
                      maxSurge:
                        default: 25%
                        description: maxSurge is the maximum number of additional
                          pods that can be created during the update.
                        type: string
                      maxUnavailable:
                        default: 25%
                        description: maxUnavailable is the maximum number of pods
                          that can be unavailable during the update.
                        type: string
                    type: object
                  type:
                    default: Recreate
                    description: type is the deployment strategy type.
                    enum:
                    - Recreate
                    - RollingUpdate
                    type: string
                type: object
              sync:
                description: sync configures periodic git repository synchronisation.
                properties:
                  enabled:
                    default: false
                    description: |-
                      enabled triggers a sync operation when set to true.
                      The controller resets it to false after sync completes.
                    type: boolean
                  time:
                    description: time is the scheduled time for the next sync operation.
                    format: date-time
                    type: string
                type: object
              webhook:
                description: webhook is an optional callback invoked when the instance
                  phase changes.
                properties:
                  url:
                    description: url to POST status updates to when the instance phase
                      changes.
                    type: string
                required:
                - url
                type: object
            required:
            - adminPassword
            - image
            - ingress
            - replicas
            type: object
          status:
            description: OdooInstanceStatus defines the observed state of OdooInstance.
            properties:
              conditions:
                description: |-
                  conditions represent the detailed state of this resource using
                  standard Kubernetes condition conventions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastBackup:
                description: lastBackup is the timestamp of the most recent successful
                  backup.
                format: date-time
                type: string
              message:
                description: message is a human-readable description of the current
                  status.
                type: string
              phase:
                description: phase is the current lifecycle phase of the instance.
                enum:
                - Running
                - Upgrading
                - Restoring
                type: string
              ready:
                description: ready is true when the instance is fully functional and
                  all pods are ready.
                type: boolean
              url:
                description: url is the primary URL at which the instance is reachable.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
